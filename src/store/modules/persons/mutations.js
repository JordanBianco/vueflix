export const GET_PERSON = (state, person) => {
    state.person = person;
}

export const GET_MOVIE_CREDITS = (state, movies) => {
    state.movies = movies;
}

export const GET_TV_CREDITS = (state, tv) => {
    state.tv = tv;
}

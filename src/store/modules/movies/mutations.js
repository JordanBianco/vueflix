export const GET_MOVIES = (state, movies) => {
    state.movies = movies;
}

export const GET_MOVIE = (state, movie) => {
    state.movie = movie;
}

export const GET_REVIEWS = (state, reviews) => {
    state.reviews = reviews;
}

export const GET_CREDITS = (state, credits) => {
    state.credits = credits;
}

export const GET_RECOMMENDATIONS = (state, recommendations) => {
    state.recommendations = recommendations;
}

export const GET_KEYWORDS = (state, keywords) => {
    state.keywords = keywords;
}